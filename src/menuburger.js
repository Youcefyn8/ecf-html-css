function afficherMenu() {
    let refMenu = document.getElementById("menu");
    refMenu.style.left="0";
}

function cacherMenu() {
    let refMenu = document.getElementById("menu");
    refMenu.style.left="-100%";
}